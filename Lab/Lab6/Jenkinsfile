pipeline {
    agent {
        label 'docker-agent'
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Run Tests') {
            steps {
                dir('Lab/Lab6') {
                    sh 'mvn clean test'
                }
            }
        }

        stage('Publish Test Results') {
            steps {
                junit 'Lab/Lab6/target/surefire-reports/*.xml'
            }
        }
    }
}